name: DB check

on: 
  push

jobs:
  pr_status:
    name: tag_get
    environment: test
    runs-on: ubuntu-latest
    steps:
     - uses: actions/checkout@v3
     - uses: actions/setup-python@v4
       with:
           python-version: '3.9'
     - name: install-databricks-cli
       run: |
          pip install databricks-cli --upgrade
          host=https://${{ secrets.DATABRICKS_INSTANCE }}
          echo $host
          echo "DATABRICKS_HOST=${{ env.DATABRICKS_HOST }}" >> $GITHUB_ENV
          echo "DATABRICKS_TOKEN=${{ env.DATABRICKS_PAT }}" >> $GITHUB_ENV  
          databricks jobs list --output json
       env:
         DATABRICKS_HOST: https://${{ secrets.DATABRICKS_INSTANCE }}
         DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_PAT }}
     - name: tesing
       run: |
          echo ${{ github.workspace }}
          ls
          pwd
          job_id=848514976127742
          cat "${{ github.workspace }}/test/testing/test.json"
          cat "${{ github.workspace }}/test/testing/test.json" | jq --arg job_id $job_id --arg group_name ${{ vars.GROUPNAME }} '.job_id |= $job_id | (.new_settings.access_control_list[] | .group_name) |= $group_name' >> reset_job.json
          if [env == "dev" ]
             cat reset_job.json
          fi
          ls
          git status
